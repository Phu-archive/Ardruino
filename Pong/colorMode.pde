class ColorMode{
  int pad_color;
  int background_color;
  int ball_color;

  ColorMode(int new_pad_color, int new_background_color, int new_ball_color){
    pad_color = new_pad_color;
    background_color = new_background_color;
    ball_color = new_ball_color;
  }
}
